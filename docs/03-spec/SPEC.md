# SPEC — Quetzalcoatl v1.2.0

> 상태: 확정 · 날짜: 2026-06-23 · 소유자: Product Planner · 승인: CHENGHAO QUAN

## 문제 정의 / 사용자 / 범위

한 세션·기기·실행 경계를 넘는 능력 추가. 사용자: 스킬 소유자(Claude Code) + 모델 중립 사용자.

### 반드시 포함

5개 능력의 SKILL.md 반영 + 문서·git·artifact 적용 + 안전 검증.

### 이번에는 제외

진짜 락 매니저/분산 합의, 실행 가능한 스케줄러, 전용 멀티에이전트 런타임.

## 기능 요구사항

| ID  | 요구사항                                               | 우선순위 | 수용 기준                                                                  |
| --- | ------------------------------------------------------ | -------- | -------------------------------------------------------------------------- |
| F1  | 지속 자율 실행(승인된 계획 범위 내, 게이트 유지)       | 必       | §1.7+§18 존재; RED/GREEN서 게이트 항목 정지; §1.1과 모순 없음              |
| F2  | 레이트리밋·하위 에이전트 회복력                        | 必       | §19 존재; 체크포인트=커밋; 외부발송 자동재개 제외; 하위 에이전트 점검 규율 |
| F3  | 문서 체계(구조·마크다운·그림/표·용어 부록)             | 必       | §6.1/§6.2 구조 규칙; `docs/` 실제 적용; GLOSSARY 부록                      |
| F4  | Git 프로토콜(init 가드·Conventional·SemVer 태그)       | 必       | §20 존재; 본 작업이 Conventional 커밋·`v1.2.0` 태그로 수행됨               |
| F5  | 핸드오프 대시보드(공유 상태·단일 라이터/CAS·SSOT=repo) | 必       | §7.1+§21 존재; 작업 보드/소유자/재개 지점 필드; live artifact 읽기 미러    |
| F6  | 모델 중립 강등(각 기능에 비-Claude-Code 동작)          | 必       | F1~F5 각각 "환경별 동작" 절 존재                                           |
| F7  | 버전·매니페스트·문서 일관 갱신                         | 必       | frontmatter/plugin/marketplace=1.2.0; README·CHANGELOG 갱신                |

## 비기능 요구사항

- 보안: §1.6 게이트 불변, 자율로 약화 금지.
- 정직성: 강제 안 되는 메커니즘을 "보장"으로 표기 금지(§15).
- 호환성: 기존 §0~§17·모드 표 보존. frontmatter ≤ 1024자(현재 856).
- 모델 중립: 파일/git/artifact 없는 환경서 텍스트로 강등.

## 엣지 케이스 / 실패 처리

- 기존 repo에 `git init` 시도 → 가드로 확인.
- 레이트리밋 중 부분 커밋 → 재개 전 중복 확인.
- 동시 claim → git 충돌로 표면화, 먼저 안착 커밋 우선.
- ChatGPT서 Mermaid 미렌더 → 코드블록 텍스트로 수용.

## 사람 승인 게이트

배포(푸시·태그)는 사전승인됨(A2). 플러그인 재설치 시점은 사람 판단.

## 미해결 질문

실사용 멀티에이전트 동시 작업 빈도, ChatGPT 경로의 수동 재개 부담.
